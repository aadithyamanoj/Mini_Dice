# VCS
simv
simv.daidir/
csrc/
build/

# Verilator
obj_dir/

# Verdi
verdi/
verdiLog/
novas_dump.log
novas.conf
novas.rc
sysBusyP*
sysProgressP*

# Simulation artifacts
*.fsdb
*.fsdb.*
*.vpd
*.vcd
*.log
*.key

# Synthesis
*.db
*.DB
*.sdb

# OS / Editor
.DS_Store
*.swp
*~

/tb/cgra_core/verification/vc_hdrs.h

# Locally-cloned external trees (not submodules of Mini_Dice).
# basejump_stl is referenced via $BSG_HOME at compile time; tracking it
# in git here would require pulling in its corrupted nested submodules.
/rtl/basejump_stl/